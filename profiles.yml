weather_analytics:
  target: dev
  outputs:
    dev:
      type: duckdb
      path: "data/weather_analytics.duckdb"
      schema: main
      threads: 4
    prod:
      type: duckdb
      path: "data/weather_analytics.duckdb"
      schema: main
      threads: 4
